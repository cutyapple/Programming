// const fs = require('fs');
// const number = fs.readFileSync('/dev/stdin').toString().split(" ").map(n => Number(n));

const number = [3, 4, 6];

console.log(parseInt(number[2] / number[1]), number[2] % number[1]);
