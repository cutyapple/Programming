// const fs = require('fs');
// const numbers = fs.readFileSync('/dev/stdin').toString().split(" ").map(v => Number(v));

const numbers = [4, 3];

console.log((numbers[0] + numbers[1]) * (numbers[0] - numbers[1]));
